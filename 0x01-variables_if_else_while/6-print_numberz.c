#include <stdio.h>
/**
 * main - Prints single digit numbers of base 10 starting from 0
 * followed by a new line
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (n = 0; n<= 10; n++)
	{
		putchar((n + 48);
	}
	putchar('\n');
	return (0);
}
